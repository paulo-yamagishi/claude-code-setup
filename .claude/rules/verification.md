# Verification Rules (always active)

- All JSON examples must be valid JSON — no comments, no trailing commas
- All file paths must be correct relative paths
- Settings keys and hook events must match official documentation at code.claude.com/docs
